import { Color } from '../../constants/Color';
import styled from 'styled-components/native';

const TopTabBarTextItem = styled.Text`
  font-size: 20px;
  font-weight: 600;
  color: ${Color.Gray2};
`;

export default TopTabBarTextItem;
